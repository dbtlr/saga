{
  "name": "@saga/cli",
  "version": "0.1.0-next",
  "private": true,
  "bin": {
    "saga": "./bin/saga.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "saga": "tsx src/main.ts",
    "test": "vp test run --passWithNoTests"
  },
  "dependencies": {
    "@saga/collectors": "workspace:*",
    "@saga/db": "workspace:*",
    "@saga/mcp": "workspace:*",
    "@saga/runtime": "workspace:*",
    "@saga/service": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "effect": "^3.21.4",
    "postgres": "^3.4.9",
    "tsx": "^4.22.4"
  }
}
