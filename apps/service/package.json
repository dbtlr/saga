{
  "name": "@saga/service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "migrate": "tsx src/migrate.ts",
    "start": "tsx src/main.ts",
    "test": "vp test run --passWithNoTests"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.6",
    "@saga/db": "workspace:*",
    "@saga/runtime": "workspace:*",
    "effect": "^3.21.4",
    "hono": "^4.12.27",
    "tsx": "^4.22.4"
  },
  "devDependencies": {
    "@saga/api-client": "workspace:*"
  }
}
