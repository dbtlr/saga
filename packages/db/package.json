{
  "name": "@saga/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "db:generate": "drizzle-kit generate --config drizzle.config.ts",
    "db:migrate": "drizzle-kit migrate --config drizzle.config.ts",
    "db:test:down": "docker compose -f docker-compose.test.yml down -v",
    "db:test:up": "docker compose -f docker-compose.test.yml up -d --wait",
    "test": "vp test run --passWithNoTests",
    "test:postgres": "vp test run src/consolidation-records.postgres.test.ts src/database.postgres.test.ts src/job-runs.postgres.test.ts src/raw-session-import.postgres.test.ts src/session-embeddings.postgres.test.ts src/session-recall.postgres.test.ts src/session-safety.postgres.test.ts",
    "test:postgres:local": "SAGA_TEST_DATABASE_URL=postgres://saga:saga@localhost:55432/saga_test vp test run src/consolidation-records.postgres.test.ts src/database.postgres.test.ts src/job-runs.postgres.test.ts src/raw-session-import.postgres.test.ts src/session-embeddings.postgres.test.ts src/session-recall.postgres.test.ts src/session-safety.postgres.test.ts"
  },
  "dependencies": {
    "@saga/claims": "workspace:*",
    "@saga/contracts": "workspace:*",
    "@saga/runtime": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "effect": "^3.21.4",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
